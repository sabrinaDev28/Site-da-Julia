// Função para redirecionar para a próxima página
function goToGallery() {
    window.location.href = "galeria.html"; // Nome do arquivo da próxima página
  }

// Função para virar a carta e mostrar a frase
function flipCard(card) {
  card.classList.toggle('flip'); // Adiciona ou remove a classe 'flip' que ativa o efeito de virada
}
